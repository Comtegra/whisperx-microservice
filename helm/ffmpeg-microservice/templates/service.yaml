apiVersion: v1
kind: Service
metadata:
  name: {{ include "ffmpeg-microservice.fullname" . }}
  labels:
    {{- include "ffmpeg-microservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ffmpeg-microservice.selectorLabels" . | nindent 4 }}
